<!doctype html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0,
           minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>{{title}}</title>
    <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css">
    <link rel="icon" href="/Images/cv_generator_log.png"  type="image/png">

    <script src="/script/jQuery.js"></script>
</head>

<style>
    * {
        margin: 0px;
        padding: 0px;
        box-sizing: border-box;
    }

    .index-container {
        height: 100vh;
        width: 100vw;
        background-color: white;
    }

    .container {
        background-color: white;
    }
    body {
        padding: 10px;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
    }

    #home {
        transition-property: padding;
        transition-duration: .5s;

    }

    #home:hover {
        padding-left: 30px;
        padding-right: 30px;
    }

    .dialog-container {
        position: fixed;
        width: 40vw;
        min-height: 60%;
        padding: 50px;
        top: -80%;
        left: 30vw;
        margin-left: auto;
        z-index: 5;
        border-radius: 30px;
        background-color: white;
        transition-property: top, left, z-index;
        transition-duration: .5s;
        transition-timing-function: ease-in-out;
    }

    .close-container h4 {
        border-radius: 10px;
        padding: 5px 20px;
    }

    .loader {
        width: 40px;
        height: 40px;
        border: 5px solid green;
        border-left-color: transparent;
        border-right-color: transparent;
        border-radius: 50%;
        animation: .5s ease infinite alternate spinner;
    }

    @keyframes spinner {
        form {
            transform: rotate(360deg) scale(1);
        }

        to {
            transform: rotate(1turn) scale(1.2);
        }
    }

    @media screen and (max-width: 768px) {
        .dialog-container {
            width: 90vw;
            left: 5vw;
            padding: 20px;
        }
    }

    @media screen and (min-width: 768px) and (max-width: 992px) {
        .dialog-container {
            width: 60vw;
            left: 20vw;
        }
    }

    @media screen and (min-width: 992px) {
        .dialog-container {
            width: 60vw;
            left: 20vw;
        }
    }
</style>

<body>
    <header style="width: 100%; min-height: 100px; border-radius: 10px;"
        class="bg-primary flex-wrap d-flex justify-content-start align-items-center px-5 w-100">

        <a id="home" href="/" class="d-flex text-white text-decoration-none align-items-center gap-3">
            <img src="/Images/cv_generator_log.png" alt="CV Generator Logo" height="60px">
            <div class="right-a d-flex flex-column">
                <h4 class=""> CV Generator
                </h4>
                <p>{{email}}</p>
            </div>
        </a>

        <div class="center ms-auto d-flex gap-3 px-3">
            <a href="/" class="d-flex text-white text-decoration-none align-items-center ms-auto h-100">
                <h6>Home</h6>
            </a>
            <a href="/blogs" class="d-flex text-white text-decoration-none align-items-center ms-auto h-100">
                <h6>Blogs</h6>
            </a>

        </div>

        <button id="login" class="btn d-flex text-white text-decoration-none align-items-center h-100">
            <h6>Login</h6>
        </button>

        <form
            class="dialog-container d-flex flex-column justify-content-between align-items-center border border-2 border-primary "
            method="post" action="/login" id="form">

            {{!-- Header container with login text and close button --}}
            <div class="close-container w-100 d-flex justify-content-center">
                <h1 class="text-center grow-5 text-primary">Login</h1>
                <h4 id="close-button"
                    class="btn btn-danger ms-auto border border-2 text-center d-flex align-items-center justify-content-center">
                    X</h4>
            </div>

            {{!-- Message container --}}
            <div class="message-container w-75 d-flex justify-content-center align-items center ">
                <h5 class="text-primary text-center" id="message"> Please Enter your email and password to enter
                    dashboard </h5>
            </div>

            {{!-- Email container --}}
            <div class="email-container w-75">
                <label for="email" class="form-label fw-bold">Email</label>
                <input type="text" class="form-control fs-5" name="email" id="email">
            </div>

            {{!-- Password container --}}
            <div class="password-container w-75">
                <label for="password" class="form-label fw-bold">Password</label>
                <input type="password" class="form-control fs-5" name="password" id="password">
            </div>

            {{!-- Sign up button container --}}
            <div class="sign-up-container w-75 d-flex flex-column justify-content-start py-2">
                <a href="/signup">
                    Don't you have an existing account?<br>
                    <strong>Sign up Here</strong></a>
            </div>
            </div>

            {{!-- Submit button container --}}
            <div class="submit-container w-100 d-flex justify-content-center align-items-center gap-3">
                <div class="loader d-none">

                </div>
                <input id="submit-button" type="submit" value="Submit" class="btn btn-success">
            </div>

        </form>

    </header>

    <section class="container py-3">
        {{{body}}}
    </section>

    <footer style="background-color:#f3f3f3; min-height:100px; border-radius: 20px; padding: 20px;"
        class="container d-flex justify-content-center align-items-center">
        ©Copywright 2024<br>
        Developed by : Dere boy

    </footer>

    <script src="/bootstrap/js/bootstrap.min.js"></script>

    <script src="/script/index.js"></script>

</body>

</html>